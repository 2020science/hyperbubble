CREATE TABLE IF NOT EXISTS scores (board TEXT NOT NULL, handle TEXT NOT NULL, day INTEGER NOT NULL DEFAULT 0, year INTEGER NOT NULL, hills INTEGER NOT NULL DEFAULT 0, flo INTEGER NOT NULL DEFAULT 0, glyphs TEXT NOT NULL DEFAULT '', ts INTEGER NOT NULL, PRIMARY KEY (board, handle, day));
